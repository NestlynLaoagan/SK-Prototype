# Database Folder

This folder is intended for database-related files.

For this project, Firebase is used as the database, and its related files (configuration, hooks, providers) are located in the `src/firebase` directory.

The database structure is defined in `docs/backend.json`, and security rules are in `firestore.rules`.
